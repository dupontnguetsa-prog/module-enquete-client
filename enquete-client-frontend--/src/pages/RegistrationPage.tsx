import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../api';import {useAuth} from '../context/AuthContext';import '../styles/auth.css'
export default function RegistrationPage(){const nav=useNavigate();const {refresh}=useAuth();const [f,setF]=useState({nom:'',email:'',identifiant:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{await api('/api/auth/register',{method:'POST',body:JSON.stringify({...f,email:f.email||null})});if(await refresh())nav('/bureau')}catch(err){setError((err as Error).message)}finally{setBusy(false)}}return <div className="auth-page"><div className="auth-brand"><img src="/logo-afriland.png" alt="Afriland First Bank" /></div><div className="auth-visual"><img className="auth-background-image" src="/background1.png" alt="" /><div className="code-orbit code-orbit-one">&lt;/&gt;</div><div className="code-orbit code-orbit-two">{'{ }'}</div><span className="eyebrow">NOUVEL ESPACE</span><h1>Créez votre<br/><em>espace de travail.</em></h1><p>Un environnement personnel pour piloter vos campagnes de feedback de bout en bout.</p><div className="team-showcase"><div className="team-heading"><span className="team-pulse"/>Équipe de confiance <small>EN LIGNE</small></div><div className="team-avatars"><span>JD</span><span>AM</span><span>SK</span><span>+8</span></div><div className="team-status"><strong>Collaboration fluide</strong><small>Vos données, vos équipes, un espace sécurisé.</small></div></div></div><div className="auth-card"><div className="eyebrow">INSCRIPTION</div><h2>Créez votre compte</h2><p className="sub">Quelques informations pour commencer.</p><form onSubmit={submit}><label>Nom complet<input value={f.nom} onChange={e=>setF({...f,nom:e.target.value})} placeholder="Votre nom"/></label><label>Adresse e-mail <span>(facultatif)</span><input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} placeholder="vous@entreprise.com"/></label><label>Identifiant<input value={f.identifiant} onChange={e=>setF({...f,identifiant:e.target.value})} placeholder="Votre identifiant"/></label><label>Mot de passe<input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})} placeholder="8 caractères minimum"/></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary auth-submit" disabled={busy}>{busy?'Création...':'Créer mon espace →'}</button></form><div className="auth-footer">Vous avez déjà un compte ? <button onClick={()=>nav('/identification')}>Se connecter →</button></div></div></div>}
